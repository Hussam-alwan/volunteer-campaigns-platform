# In production the Vite proxy doesn't exist — the dist folder is just
# static files. axios calls the absolute backend URL directly. The
# backend must allow CORS for the frontend's origin with credentials.
VITE_API_BASE_URL=https://sbc-production.up.railway.app/api/v1
