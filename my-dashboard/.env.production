# Docker setup: nginx serves the dist folder and proxies /api/ and /uploads/
# to the backend container (see nginx.conf). Same-origin paths keep API
# calls and uploaded photos consistent — both reach the local backend.
VITE_API_BASE_URL=/api/v1
VITE_SERVER_BASE_URL=
